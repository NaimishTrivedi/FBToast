<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:paddingTop="32dp"
    android:paddingBottom="32dp">

    <RelativeLayout
        android:id="@+id/relContainer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:background="@drawable/bg_native_toast">

        <ImageView
            android:id="@+id/toastImg"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_marginEnd="10dp"
            android:layout_alignParentTop="true"
            android:layout_alignParentStart="true"
            tools:src="@drawable/fbtoast_done"
            android:contentDescription="@null"/>

        <TextView
            android:id="@+id/toastMsg"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:textColor="@color/fbtoast_white"
            tools:text="Sample Message"
            android:textSize="15sp"
            android:layout_toEndOf="@id/toastImg" />

    </RelativeLayout>

</RelativeLayout>